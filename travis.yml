language: python
sudo: required
cache: false
python:
    - '3.7'

addons:
  apt:
    packages:
      - ca-certificates

install: "make init"

script:
  - echo "Unit tests"
  - make test

after_script:

  # start the web app
  - |
    make run-prod
